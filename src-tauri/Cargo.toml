[package]
name = "xiaoxin-sticky-note"
version = "1.0.0"
description = "XiaoxinStickyNote 桌面便签应用"
authors = ["you"]
edition = "2021"

# 关键：release 必须启用 custom-protocol，否则 generate_context! 会把构建当作 dev 模式，
# 不嵌入前端资源（指向 devUrl），导致发布版 webview 加载失败（空白/错误页）。
[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-fs = "2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-dialog = "2"
tauri-plugin-single-instance = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
uuid = { version = "1", features = ["v4"] }
chrono = "0.4"
reqwest = { version = "0.12", features = ["json"] }
arboard = "3"
# 透明主题原生亚克力：SetWindowCompositionAttribute（未文档化 API，运行时 GetProcAddress）
# 圆角由 CSS border-radius 负责，不再使用 DWM 圆角（见 main.rs 说明）
windows-sys = { version = "0.59", features = ["Win32_Graphics_Gdi", "Win32_Foundation", "Win32_System_LibraryLoader"] }
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }
